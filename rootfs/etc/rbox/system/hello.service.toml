[Unit]
Description = "Hello service (test ExecStop)"
After = []
Requires = []

[Service]
Type = "simple"
ExecStart = "/bin/rbox echo hello-service-started"
ExecStop = "/bin/rbox echo hello-service-stopping"

[Install]
WantedBy = ["default.target"]
