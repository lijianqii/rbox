[Unit]
Description = "Persistent log forwarder (kmsg -> /var/log/messages)"
Name = "logkeeper"

[Service]
Type = "simple"
ExecStart = "/bin/rbox logkeeper"
Restart = "always"
RestartSec = 1

[Install]
WantedBy = ["default.target"]