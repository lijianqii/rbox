[Unit]
Description = "Long-running test service (rservice start/stop/restart)"
Name = "longrun"

[Service]
Type = "simple"
ExecReload = "/bin/rbox echo reloaded-ok"
ExecStart = "/bin/rbox sleep 100"

[Install]
WantedBy = ["default.target"]
