[Unit]
Description = "Starts template instance (test)"
Name = "inst-starter"
Wants = ["inst@alpha"]

[Service]
Type = "simple"
ExecStart = "/bin/rbox sleep 60"

[Install]
WantedBy = ["default.target"]
