[Unit]
Description = "Console shell (test override: direct shell, no login)"
Name = "console-shell"

[Service]
Type = "simple"
ExecStart = "/bin/sh"
Console = true
# 标记 console shell 环境，集成测试用它验证 shell respawn 后配置保留
Environment = ["RBOX_CONSOLE=1"]

[Install]
WantedBy = ["default.target"]