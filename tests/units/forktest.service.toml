[Unit]
Description = "Forking test service (Type=forking, daemonizes in 1s)"
Name = "forktest"

[Service]
Type = "forking"
ExecStart = "/bin/rbox sleep 1"
Restart = "on-failure"
RestartSec = 1
TimeoutStartSec = 5

[Install]
WantedBy = ["default.target"]
