[Unit]
Description = "Hello service (test Environment/ExecStop)"
Name = "hello"

[Service]
Type = "simple"
ExecStart = "/bin/rbox env"
Environment = ["HELLO=world"]
ExecReload = "/bin/rbox echo reloaded-ok"
ExecStop = "/bin/rbox echo hello-service-stopping"

[Install]
WantedBy = ["default.target"]
