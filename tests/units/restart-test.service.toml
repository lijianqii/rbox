[Unit]
Description = "Restart test (always fails, Restart=on-failure)"
Name = "restart-test"

[Service]
Type = "simple"
ExecStart = "/bin/rbox false"
Restart = "on-failure"

[Install]
WantedBy = ["default.target"]
